# Origin Kind and Format replace the compound mode name

`Project.mode` encoded input and output in one string (`x-to-vertical`). Adding upload as a second input and any second output format would need one name per combination, so a Clip now carries Origin Kind and Format independently. Mode survives as a front-end concept only: a card on the home page pairing the two, not a property of a Clip.
